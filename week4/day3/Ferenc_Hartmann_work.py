class Needtest():
    def get_apple(Self):
        return "apple"

class Summarize():
    def sum(Self, a=[0]):
        summed = 0
        for i in range(len(a)):
            summed += a[i]
        return summed

class Anagram():
    def sum(Self, a=[0]):
        summed = 0
        for i in range(len(a)):
            summed += a[i]
        return summed
